##
## Cross-compilation configuration for cross-rs
## Used by the CI/CD release workflow for aarch64-unknown-linux-gnu builds
##

[target.aarch64-unknown-linux-gnu.env]
passthrough = ["OPENSSL_STATIC", "OPENSSL_NO_PKG_CONFIG"]
